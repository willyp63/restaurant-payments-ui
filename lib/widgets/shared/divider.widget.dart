import 'package:flutter/material.dart';

import 'package:mimos/theme/colors.dart';

class MMSDivider extends StatelessWidget {
  @override
  Widget build(context) {
    return Container(height: 1, color: MMSColors.warmGray);
	}
}
